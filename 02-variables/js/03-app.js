// la tercera forma de crear variables es const
// existen pocas diferencias entre const y let, asi que vamos a verlas
// Primero una variable const su valor no puede ser reasignado.

const producto = "Monitor 30 pulgadas";
console.log(producto);
producto = "Monitor 27 pulgadas"; // NO se puede reasignar la variable
console.log(producto);

// Las variables const deben iniciarse con un valor
//const precio; //Genera un error
precio = 20;
console.log(precio);

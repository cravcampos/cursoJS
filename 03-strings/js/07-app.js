const producto = "Monitor de 29 pulgadas";

// toUpperCase() permite cambiar un texto todo a mayusculas
console.log(producto.toUpperCase());

// toLowerCase() permite pasar todo el texto a minusculas
console.log(producto.toLowerCase());

// toString() pasar una variable a tipo string
const precio = 2000;
console.log(precio);
console.log(precio.toString());

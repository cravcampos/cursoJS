// las estructuras de control, se utilizan para definir que parte del codigo deseamos que se ejecute basados en una condición

// Operador If

const puntaje = "1000";

if (puntaje == 1000) {
  console.log("Si es igual!!!"); // Si la condicion se cumple se ejecuta esto
} else {
  console.log("No es igual!!!"); // Si la condicion no se cumple ejecuta esto
}

const numero = 30;
const numero1 = 20;

let resultado;

// se conocen como operadores, sirven para realizar sumas, restas, multiplicaciones, divisiones, modulos

// Suma +
resultado = numero + numero1;
console.log(resultado);

// Resta -
resultado = numero - numero1;
console.log(resultado);

// Multiplicación *
resultado = numero * numero1;
console.log(resultado);

// Division /
resultado = numero / numero1;
console.log(resultado);

// Modulo %
resultado = numero % numero1;
console.log(resultado);
